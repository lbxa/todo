# The origin the app is deployed to. Used for absolute OpenGraph/Twitter URLs,
# so link previews resolve. Without it they point at localhost and won't render.
#   e.g. https://todo.<your-subdomain>.workers.dev  or  https://todo.example.com
NEXT_PUBLIC_SITE_URL=
