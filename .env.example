# Overrides the origin used for absolute OpenGraph/Twitter URLs.
# Only needed for preview deployments on another origin; production defaults
# to https://todo.lbxa.net (see app/layout.tsx).
NEXT_PUBLIC_SITE_URL=
